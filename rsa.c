#include "rsa.h"
/**
 * main - Entry point to the program
 * 
 * @args: the number of arguments entered to the program
 * @argv: an array containing the names of the arguments passed
 * 
 * Return: 0 for success
 */
int main(int args, char argv[])
{

    return (0);
}